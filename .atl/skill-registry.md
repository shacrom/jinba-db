# Skill Registry — jinba-db

Auto-generated by `/sdd-init` on 2026-04-20.
This file is consumed by the orchestrator to inject pre-resolved compact rules into sub-agent prompts.

## Project Scope

- **Repo**: jinba-db
- **Purpose**: source of truth for the Supabase schema consumed by the sibling Jinba repos.
- **Stack**: Postgres (via Supabase), SQL migrations, TypeScript (for generated types), GitHub Actions.
- **Sibling repos**: `jinba-web` (Astro), `jinba-scraper` (Node worker).

## User Skills — Trigger Table

| Trigger phrase / situation | Skill | When to use |
|---|---|---|
| "open PR", "create pull request", "prepare for review" | `branch-pr` | Before opening a PR for a change |
| "open issue", "report bug", "feature request" | `issue-creation` | When filing a GitHub issue |
| "dual review", "adversarial review", "juzgar", "judgment day" | `judgment-day` | Parallel adversarial review of a target before merge |
| "save to memory", decisions/conventions/discoveries | `engram:memory` | ALWAYS active; save proactively after decisions, bug fixes, conventions |
| "security review" | `security-review` | Security review of pending changes on branch |
| "review PR" | `review` | Reviewing a pull request |
| "simplify" | `simplify` | Review changed code for reuse, quality, efficiency |

## Project Conventions

(None yet — jinba-db was initialized empty. Conventions will be established in the first SDD change, `db-schema-core`.)

Expected conventions once scaffolded:
- Migrations in `supabase/migrations/` with `YYYYMMDDHHMMSS_slug.sql` naming.
- Seeds in `supabase/seeds/` (makes, models, generations for MVP).
- Generated types in `types/database.ts` via `supabase gen types typescript --linked`.
- Biome for linting/formatting TS scripts.
- CI: validate migrations via `supabase db diff` on PR.

## Compact Rules (inject into sub-agents that touch this repo)

- **Migrations are immutable once merged to main** — never edit an existing migration; always add a new one.
- **Never write schema changes outside of `supabase/migrations/`** — the repo is the source of truth.
- **Generated types (`types/database.ts`) MUST be committed** after every schema change so sibling repos can import them via direct clone or paquete.
- **Seeds must be idempotent** — use `ON CONFLICT DO NOTHING` or equivalent.
- **PII constraint (project-wide)**: columns that could hold personal data (phone, email, seller name) MUST NOT exist in the `listings` tables; only anonymized hashes allowed.

## External References

- User-level CLAUDE.md: `/Users/marmibas/.claude/CLAUDE.md` (Engram protocol, Agent Teams Lite orchestrator).
- Engram project key: `jinba-db` (SDD artifacts for this repo). Broader project-wide context uses key `jinba`.
